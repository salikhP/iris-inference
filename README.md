# ai-infra-base · Iris Inference API (MLflow + FastAPI)

A minimal, production-like project that demonstrates a complete MLOps flow:
training a simple ML model, logging it to MLflow, and serving predictions through a FastAPI REST API.

---

## Tech Stack
- **Python 3.12** + **Poetry**
- **scikit-learn**, **pandas**, **numpy**
- **MLflow** (for experiment tracking & model registry)
- **FastAPI** + **Uvicorn** (for inference API)
- **Docker** + **docker-compose** (for containerization)
